# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **주 사용자:** League of Legends 내전(커스텀 게임)을 정기적으로 하는 사람들. 특정 지인 그룹에 한정하지 않고 누구나 가입해 쓰는 공개 서비스다.
- **그룹장(OWNER):** 그룹을 만들고 초대 코드를 배포하며, 내전을 열고 팀을 확정하고, 결과를 입력하고, 디스코드 연동(웹후크·봇)을 설정한다.
- **그룹원(MEMBER):** 라이엇 계정을 연동하고 선호 라인을 지정한 뒤, 내전에 참가하고 결과 후 팀원을 평가하며, 자신과 다른 그룹원의 티어·전적·MMR 추이를 확인한다.
- **상황:** 대부분 디스코드에 모여 게임을 하면서 PC 브라우저로 쓴다. 결과 입력·전적 확인 등은 모바일에서도 자주 일어난다.

## Product Purpose

친구들끼리 하는 LoL 내전을 "팀 짜기부터 전적까지" 한 곳에서 끝낸다. 그룹원의 라이엇 전적을 기반으로 그룹 내부 티어를 매기고, AI가 MMR과 선호 라인을 고려해 균형 잡힌 팀을 자동 구성하며, 팀 배정 → 경기 결과 → MMR 변동 → 전적·통계까지 이어서 관리한다.

성공: 내전 한 판을 여는 데 드는 수고(팀 밸런스 논쟁, 수동 기록)가 사라지고, 그룹이 계속 DS_LOL로 내전을 돌린다.

## Positioning

특정 기능 하나가 아니라 **흐름 전체**가 차별점이다. 라이엇 전적 연동 → 그룹 전용 1~5 티어와 내부 MMR → AI 자동 팀 구성(밸런스 근거·예상 승률 제시) → 결과 입력과 MMR 반영 → 매너 평가 → 전적·통계 → 디스코드 알림/슬래시 명령어까지 하나의 루프로 연결되어 있다. 어느 한 단계를 빼면 성립하지 않는다.

## Operating Context

- 로그인은 Google OAuth. 첫 로그인 후 온보딩(`/onboarding`)에서 프로필과 게임 계정(Riot ID: gameName#tagLine)을 연동한다. 연동 직후 전적이 자동 동기화된다.
- 그룹은 초대 코드로 가입한다. 한 사용자가 여러 그룹에 속할 수 있고, 티어·MMR 추이는 그룹별로 분리된다.
- 내전 상태 흐름: `WAITING`(참가자 선택) → `MATCHED`(팀 생성·수동 조정) → `FINISHED`(승리 팀 입력, MMR 변동, 팀원 평가).
- 팀 표기: `TEAM_A` = 레드, `TEAM_B` = 블루.
- 디스코드: 그룹장이 웹후크를 설정하면 팀 구성 완료/내전 종료 알림이 가고, 봇을 서버에 초대하면 `/티어표` 등 슬래시 명령어가 해당 그룹과 자동 연동된다.
- 사용 환경: 주로 PC 데스크톱 브라우저, 모바일도 중요한 사용 경로.

## Capabilities and Constraints

- **주요 화면:** 로그인/홈, 개인정보 처리방침, 온보딩(프로필 설정), 그룹 목록, 그룹 관리, 티어표, 내전 만들기, 팀 구성, 내전 결과, 내전 기록, 통계, 사용자 프로필.
- **용어:** 내전, 그룹장/그룹원, 내부 티어(1~5, 그룹 기준)와 공식 티어(라이엇 랭크 문자열, 예: "PLATINUM III")는 서로 다른 값이다. 라인은 내전 배정용 `TOP/JUG/MID/ADC/SUP`와 라이엇 원본 `TOP/JUNGLE/MIDDLE/BOTTOM/UTILITY`가 구분된다. 주 라인/부 라인, 매너 점수, 밸런스 %, 예상 승률.
- **데이터 제약:** 전적·챔피언 통계는 동기화된 경기 기준이며 계정 평생 기록이 아니다. 계정 미연동·전적 미동기화 사용자는 티어/라인/MMR이 `null`이고, 이를 가짜 값으로 채우지 않는다.
- **게임 지원:** 현재 LoL 전용. 발로란트는 계정 연동까지만 되고 내전 만들기에는 비활성 칩으로 노출된다. **발로란트 정식 지원(내전까지)은 예정**되어 있으므로 게임별 분기를 전제로 설계한다.
- **스택:** React 18 + TypeScript + Vite, styled-components(테마 토큰 `src/styles/theme.ts`), TanStack Query, react-router, Vercel 배포. 한글 표시는 Pretendard.
- **외부 검수 제약:** Google OAuth 앱 검수 때문에 `/`는 로그인 페이지로 리다이렉트하지 않고 앱 이름·목적·개인정보 처리방침 링크를 직접 보여줘야 한다. 로그인 화면의 앱 이름 "DS_LOL"은 OAuth 콘솔 등록명과 철자·대소문자가 일치해야 한다. 수집 데이터(Google 이메일·프로필) 활용 목적 안내를 유지한다.
- **라이엇 API:** 라이엇 도메인 인증 파일(`public/riot.txt`)을 유지한다.

## Brand Commitments

- 이름: **DS_LOL** (표기 고정).
- 태그라인: "친구들과 하는 내전, 팀 짜기부터 전적까지".
- 말투: 한국어, 친근한 해요체(예: "~할 수 있어요", "~구성해줘요").
- 로고: `public/logo.png`, `public/logo-512.png`.

## Evidence on Hand

- 서비스 설명 카피: `index.html` meta/OG 설명, `src/pages/LoginPage.tsx`.
- 개인정보 처리방침: `src/pages/PrivacyPage.tsx`.
- 로고 에셋: `public/logo.png`, `public/logo-512.png`.
- **없음:** 사용자 후기, 이용자 수, 그룹 수, 제휴·언론 보도 등은 확인된 자료가 없으므로 만들어내지 않는다.

## Product Principles

1. **내전 한 판의 흐름이 끊기지 않게.** 참가자 선택 → 팀 구성 → 결과 → 평가가 다음 행동으로 자연스럽게 이어져야 한다.
2. **팀 밸런스는 근거와 함께.** 자동 구성 결과는 MMR 합·평균, 예상 승률, 밸런스 근거를 보여주고, 사람이 직접 조정할 수 있어야 한다.
3. **실제 데이터만 보여준다.** 연동·동기화되지 않은 값은 비어 있음을 정직하게 드러내고 추정치로 채우지 않는다.
4. **그룹이 기준이다.** 티어·MMR·통계는 그룹 맥락 안에서 의미를 가지며, 그룹 간 데이터가 섞이지 않아야 한다.
5. **디스코드와 함께 쓰는 도구.** 사용자는 디스코드에서 모이므로 웹과 디스코드 연동이 서로를 보완해야 한다.
